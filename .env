# .env file for Qobuz Squid Downloader

# Qobuz API Configuration (as used by config.py and passed to squid.wtf)
# If the default in config.py is sufficient, you might not need these.
# Only uncomment and change if you're using a different API endpoint or user agent.
 QOBUZ_API_BASE_URL="https://eu.qobuz.squid.wtf"
# QOBUZ_API_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"
# QOBUZ_CDN_USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"

# Download Directory
# This specifies where the downloaded music files will be saved relative to the script.
# For Docker, this will be relative to the /app directory inside the container.
# Make sure to map a Docker volume to this path for persistent storage (e.g., -v ./my_downloads:/app/downloads).
DOWNLOAD_BASE_DIR="downloads"

# MusicBrainz Configuration
# IMPORTANT: Change this to include your actual email address!
# This is crucial for adhering to the MusicBrainz API usage policy.
MUSICBRAINZ_USER_AGENT="QobuzSquidDownloader/1.0 (mowsonben@gmail.com)"

# AcoustID Configuration
# REQUIRED for AcoustID lookup (MusicBrainz fallback for tagging).
# Get your API key from: https://acoustid.org/api-key
ACOUSTID_API_KEY="W7tgCQytyH"

LOGGINGLOGGING_ENABLED="true"